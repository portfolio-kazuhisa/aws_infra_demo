name: Terraform CI/CD

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  preview:
    name: "initial"
    runs-on: ubuntu-latest
    steps:
      - run: echo "initial"